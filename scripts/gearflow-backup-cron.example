# Run as the Linux user that owns the Gear Flow project.
# Edit paths before installing with: crontab scripts/gearflow-backup-cron.example

0 3 * * * cd /opt/gear-flow && . .venv/bin/activate && . .env && python manage.py backup_postgres >> /var/log/gearflow-backup.log 2>&1
